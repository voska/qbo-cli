version: "2"
run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - misspell
  exclusions:
    paths:
      - docs

formatters:
  enable:
    - gofmt
